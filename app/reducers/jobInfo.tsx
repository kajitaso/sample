import { TextConsts } from '../consts'

const jobInfo = (state = TextConsts.JOB_INFO, action) => {
    switch (action.type) {

      default:
          return state
    }
  }
  
  export default jobInfo